# Issues — 論点（決めないといけないこと）

> **判断が必要な論点だけ**を置く。**やることが明確な作業は書かない**（それは [todo.md](todo.md)）。
> 論点は**決めれば消える**。作業は**やれば消える**。別物なので混ぜない。
>
> 決着したら「決着した論点」へ移す。**このファイルの中で完結する**（`todo.md` へは移さない）。
> 決着の結果として作業が生まれたら、その作業は `todo.md` に書く。

## 優先度の意味

| | 意味 |
|---|---|
| **高** | 放置すると先の作業が手戻りになる。着手前に決めるべき |
| **中** | いずれ決める必要がある。今は動いているので急がない |
| **低** | 決めたほうがよいが、後回しでよい |

## 未決の論点

| 優先度 | 論点 | 何を決めるのか / 現状 | 詳細 |
|---|---|---|---|
| 中 | **周辺検索をどうツール化するか** | 「あの山は？」に答える本命。取得できることは実証済み。**AIが自由度を持って検索できる形**にしたいが、ツールの粒度・呼び出し回数の抑え方が未定。**US-2.03の後** | `pre-research/geocoding/` §5 |
| 低 | **現在地を個人情報としてどう扱うか** | ⚠️ **座標と解決後の住所の両方**がCloudWatchに残る（Lambda=東京、エージェント=us-east-1 の2箇所。`main.py` が `question[:80]` を出力しており、そこが住所ブロック）。ログ保存の方針を決める。メモ機能を作るなら必須 | `pre-research/geocoding/` §7 |

## 決着した論点

> **結論だけ**を残す。同じ議論が再燃したときに参照するため。
> **経緯は書かない** — 重要な決定は `adr/NNN_*.md` に書き、ここからは参照するだけ。

| 決定日 | 論点 | 結論 | 詳細 |
|---|---|---|---|
| 2026-08-22 | **単体ビルドをrelease／debugのどちらで作るか** | ✅ **release**。⚠️ **懸念だった「`console.log` が出るか」は出ると確定**（除去の経路が全て不在: `babel.config.js` 無し・`transform-remove-console` 未インストール・Metro既定に `drop_console` 無し・`minifyEnabled` 既定false）。ビルド済みAPKに `[vad]`/`[handsfree]` の文字列が残ることも確認済み。📌 **署名はdebugと同じキーストア**なので上書きインストールでき、保存済み設定は消えない。📌 `expo-dev-client` はrelease APKに含まれないので `package.json` に残してよい | `App/SETUP.md` |
| 2026-08-21 | **応答後にマップアプリへどう戻すか** | ✅ **設定画面で選んだアプリをLAUNCHERインテントで開く**（[adr/007](../adr/007_return_to_map_after_answer.md)）。⚠️ **`moveTaskToBack` は実機で不成立**（自分のタスクを下げるだけで**ホーム画面に落ちる**）。**却下**: マップのタスクを `moveToFront` で上げる（⚠️ `getAppTasks()` は**自分のタスクしか返さない**）／直前の前面アプリを記録（⚠️ **他アプリの前面判定はAndroid 5以降塞がれている**）。📌 **「起動し直すとナビが壊れる」という当初の懸念は実測で覆った** — LAUNCHERインテントは**既存タスクの再開**で、タスクIDが変わらない | `pre-research/handsfree/FINDINGS.md` §13.4〜13.5 |
| 2026-08-18 | **応答後にマップアプリへ自動で戻るか（US-2.04の周辺）** | ✅ **戻す。タイミングは「回答が届いたらすぐ戻り、背面で読み上げが続く」が第1候補**、成立しなければ**「読み上げ完了後に戻る」**で妥協する。⚠️ **`expo-audio` は既定で背面に回ると再生を止める**（`AudioModule.kt` の `OnActivityEntersBackground`）ので、第1候補には `setAudioModeAsync({ shouldPlayInBackground: true })` が要る。⚠️ **`moveTaskToBack()` でマップに戻るかは未検証**（ホームに落ちる可能性）。**専用のハンズフリー画面を新設する案は採らない**（既存UIで足りる） | `adr/006`「影響」 |
| 2026-08-18 | **録音終了をどうハンズフリー化するか（US-2.04続き）** | ✅ **無音検知（VAD）を採用。閾値・無音時間は実走行で詰められるよう可変にする。** `expo-audio` の `isMeteringEnabled` で `getStatus().metering` にdBFS（無音 `-160`／最大 `0`）が入ることを**Androidネイティブ実装のソースで確認済み**（`AudioRecorder.kt` が `MediaRecorder.maxAmplitude` を `20*log10(amp/32767)` に変換）。⚠️ **`maxAmplitude` は読むたびにリセットされる**ので、`getStatus()` を読む場所は1箇所に限る（`useAudioRecorderState` との併用は不可）。**却下**: タイムアウト短縮のみ（根本解決にならない）／インカムのボタン2回押し（実装が最も複雑・マイク占有中の挙動が未検証）。⚠️ **閾値の妥当値は実走行でしか決まらない**（風切り音・エンジン音） | `pre-research/handsfree/FINDINGS.md` §10〜11 |
| 2026-08-17 | **ハンズフリー起動の実装方式（US-2.04・実機で確定）** | ✅ **`android.intent.action.VOICE_COMMAND` を `MainActivity` の intent-filter で受ける方式に実装を変更。実機（Pixel 8a）で起動を確認済み。** ⚠️ **当初採用した「方式B」（`VoiceInteractionService` で既定のアシスタントRoleを取る）は実装したが、インカムのボタンからは起動できないと実機で判明**（Bluetooth HFPは`VoiceInteractionService`ではなく`ACTION_VOICE_COMMAND`をActivityへ送る。AOSP標準実装で確認）。**Roleの取得自体は無関係ではなく前提として必要**（デジタルアシスタント選択で明示的に選ぶ必要があった）。⚠️ **「Google Assistantを明け渡す代償」は 2026-08-22 に事実誤認と判明**（必要なのは `preferred activity` の解除だけ。`adr/006` 改訂） | `pre-research/handsfree/FINDINGS.md` §9 |
| 2026-08-16 | **ハンズフリー起動の方式（US-2.04・当初案）** | ⚠️ **この決定は2026-08-17に一部覆った。上の行を見ること。** ✅ 方式B（`VoiceInteractionService` で既定のアシスタントになる）を採用。**方式A（メディアボタン）**は却下（⚠️ **押しやすいボタンはメディアキーを送らず、メディアキー系は音楽操作を潰すうえ他アプリに奪われる**）。**2台構成**も却下（⚠️ **ボタンの行き先が制御不能＋インカムの仕様上、音声入力は1台のみ**）。**方式C（ウェイクワード）**も却下（⚠️ **常時マイク占有が問題。加えてインカムのマイクで拾えるかも未知**） | `pre-research/handsfree/DECISION.md` |
| 2026-08-16 | **STTがアプリ側になった分の悪用対策** | ✅ **前提が消滅。** STTは**Lambdaが呼ぶ**ことにしたので、音声もLambdaに届く。**ペイロードサイズ＝録音秒数を手前で弾ける** | `adr/002` 改訂 / `docs/01` §7 |
| 2026-08-16 | **音声をどう送るか / S3へどう置くか** | ✅ **`POST /ask-audio`（別エンドポイント）** で受け、**Lambdaが受けてS3にPUT**。既存の `POST /ask` は変えない | `docs/01` §7 |
| 2026-08-16 | **回答の音声をどう返すか** | ✅ **署名付きURL。** ポーリング応答にはテキストとURLだけを載せ、音声はS3から直接取る（テキストが先に届く・応答が軽い） | `docs/01` §7 |
| 2026-08-16 | **録音形式** | ✅ **M4A（`mpeg4`+`aac`）。** Transcribeの推奨（FLAC/WAV）はAndroidが直接出せず、**両者が重なるのがM4A**。変換不要 | `docs/01` §7 |
| 2026-08-15 | **認証方式（APIキー / Cognito）** | ✅ **APIキー + Usage Plan。** IP制限は**削除**（併用しない）。Cognitoは利用者が1人で不要、複数ユーザーに広げるなら再検討 | `adr/004` / 設計は `docs/01` §8・§9 |
| 2026-08-13 | **同期呼び出し（29秒上限）をどう外すか** | ✅ **非同期ポーリング**（SQS + DynamoDB） | `adr/001` / 設計は `docs/01a` |
| 2026-08-13 | **進行方位の算出をどこでやるか** | ✅ **Lambda側**（`lib/geo.py`）。座標→住所と同じく「LLMに渡す前に事実を確定させる」。**2点目はアプリが履歴から選ぶ**（5m以上離れた直近の点／30秒以内）。停車中・転回直後は方位なしが正常 | `docs/01b` |
| 2026-08-13 | **会話中にどの時点の位置を参照するか** | ✅ **これはAIに判断させる**（質問の意味からしか決まらないため）。手がかりとして経過時間を渡す。⚠️ 方位の「計算」をさせない方針とは別レイヤの話 | `docs/01b` |
| 2026-08-12 | **座標→住所をどこで解決するか** | ✅ **Lambdaで必ず付ける**（`@tool` にしない）。毎回必要な前提情報なので、LLMに呼ぶか判断させない | `pre-research/geocoding/` §3 |
| 2026-08-12 | **Lambdaを挟むか** | ✅ **挟む。** 音声方式1なら中継でき、さらに**LLMに渡す前に事実（住所）を確定させる場所**という役割がある | `pre-research/agentcore/AUTH.md` |
| 2026-08-12 | **音声の実現方式** | ✅ **方式1（手前でSTT）。** Nova 2 Sonic は**日本語非対応**で不採用。日本語が追加されたら再検討の価値あり | `pre-research/voice/` §1 |
| 2026-08-12 | **初回10秒前後の遅さを許容するか** | ✅ **許容する。** コールドスタートでアプリ側では縮められない。定常は2〜3秒 | `pre-research/voice/` §6 |
| 2026-07-30 | **AgentCore Memory を使うか** | ✅ **使わない。** 要件は「一問一答＋α」まで | `pre-research/agentcore/` |
