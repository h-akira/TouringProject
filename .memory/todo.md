# TODO — やること

> **やることが明確な作業**を置く。**判断が必要な論点は書かない**（それは [issues.md](issues.md)）。
>
> | 節 | 中身 |
> |---|---|
> | 次にやる | **順番が決まっている。** 上から着手する |
> | いつやるか未定 | やるのは確実だが**時期が決まっていない** |
> | 完了したもの | 日付つき・**消さない** |

## いま取り組んでいること

**インカムのボタンだけで一巡するハンズフリーを、走行で確かめる**（App **v1.39.0**）。
📌 **経路は「音声認識」として張る方式に改めた**（[adr/010](../adr/010_intercom_mic_routing.md) 改訂・
[docs/01c](../docs/01c_app_client.md) §3a）。✅ **自宅では一巡を確認済み。**
✅ **独立レビューの指摘を App v1.40.0 に反映し、自宅で確認**（**アプリが起動していない状態からの押下でも、押下→返事まで 1.64秒**で5秒の期限に間に合う）。

## 次にやる

### 1. ⚠️ 走行テスト（v1.39.0）

- **インカムのマイクで録れた音が、走行中に聞き取れるか**（⚠️ **経路が直って初めて測れる本命**）。
  **`voice_communication` を捨ててよいか**の判断材料にする（`issues.md`）。📌 **エコーが出ないかも見る**
- **ボタンだけで一巡するか**（起動・2回目で送信・次の起動）。📌 **走行後に `btroute-trace.log` を取り出す**（`App/SETUP.md`）
- ⚠️ **見つかった課題は書き出す**（前回 2026-09-13 分も未記録。📌 **外でのAPIブロック**＝403/429の疑いを含む）

### 2. ⚠️ Playへの配信を自動化する ← **優先**

⚠️ **走行テストで課題が出たとき、すぐ配れないと困る。**
📌 **調査・比較は [pre-research/play-cicd/](../pre-research/play-cicd/)。** ⚠️ **①手段 ②署名鍵をCIに置くか ③引き金** を
決めてから実装する（`issues.md`）。⚠️ **`adr/009` D（と場合により C）の改訂が要る。**

### 3. Playストアで配れるようにする（残り）

🎉 **「ビルド→署名→Play→実機」の経路は通った**（2026-09-13・[adr/009](../adr/009_play_internal_testing_release.md)）。⚠️ **残り**:

1. ⚠️ **「アプリのコンテンツ」の宣言**（未了なら埋める）
2. ⚠️ **プライバシーポリシーのURL**（**まだ無い**。位置情報とマイクを使うので要求される）
3. **テスター向けの手順書**（⚠️ 既定アシスタントの解除・APIキーの入力・戻り先アプリの選択。**アプリが入っただけでは使えない**）
4. **他のテスターを招待**

## いつやるか未定

| 作業 | 現状 | 詳細 |
|---|---|---|
| ⚠️ **リポジトリ分離後のビルドの疎通確認**（ユーザーが行う） | Agent・Backend それぞれでビルドを1回走らせ成功を見る（📌 **ARN 不変なら Agent は Backend を起動しない**のが正しい）。📌 **Backend は次の push で走る** | [adr/011](../adr/011_repository_split.md) |
| **プロンプト調整**（⚠️ **走行で聞いてから**） | **回答の長さ**（58〜173文字とばらつく。読み上げて何文字が妥当かを先に決める）・**誤変換への耐性**（「柳井」→「屋内」）・**読み上げ前提の文体**。⚠️ `Agent/` の変更なのでデプロイは us-east-1 | `pre-research/voice/` §6 |
| **2回目の押下から送信判断までの約0.6秒を詰める** | インカム→アプリの通知は 73ms で届くが、**受信処理の中でメインスレッドが約0.63秒止まる**（`Skipped 38 frames`）。⚠️ **状態の記録ではなかった**（v1.40.0 で後に回しても変わらず）。**候補は `setMode(MODE_NORMAL)` か `sendEvent`** — 前後の時刻を記録して特定する。実害小 | `App/modules/bt-audio-route/` の受信処理・`onScoLost` |
| **アイドル課金の実測** | AgentCoreは**アイドル中も課金**される。ツーリングは散発的な質問なので影響を測りたい | `CLAUDE.md` |
| **AWS Budgets による予算遮断** | ⚠️ **多層防御で唯一残った未実装**。キーとクォータは入ったが、上限に張り付き続ければ課金は積み上がる | `docs/01` §9 |
| **Usage Plan の閾値の見直し** | いまの値（2000回/日・5req/s）は**すべて仮**。実際の使い方を測って詰める | `docs/01` §9 |
| **カスタム語彙で地名の誤認識を改善** | 「柳井」→「屋内」など同音異義が落ちる。5問中4問は正解なので急がない | `pre-research/voice/` §6 |
| **方位まわりの閾値をアプリから変更できるようにする** | 5m/30秒/2分は**すべて机上の値で未検証**。⚠️ **先に実走行で詰める**（走れば定数の修正で済む可能性が高い）。可変にするなら、UIより先に `.env`（`EXPO_PUBLIC_*`）での上書きを検討 | `docs/01b` |
| **未使用の依存 `expo-glass-effect` を削除するか確認** | `src/` 内で参照が無い。`@expo/ui` 削除と同様の理由で外せる可能性（優先度低・実害なし） | `App/package.json` |

## 完了したもの

> **消さずに残す。** この粒度なら量は増えないので、到達点の記録として使う。
> 新しいものを上に足す。（**決めたことは** [issues.md](issues.md) **の「決着した論点」へ**）

| 完了日 | やったこと |
|---|---|
| 2026-09-26 | 🎉 **インカムのボタンだけの一巡が、インカムのマイクで成立**（App **v1.39.0**・自宅で確認）。⚠️ **v1.36.0 で壊れていた**（起動直後に経路が張れない・2回目の押下で止まらない・以後起動できない）。**原因は経路を「仮想通話」で張っていたこと** — インカムは返事待ちの間 codec 交渉に応じず、⚠️ **通話中のボタンは「電話を切る」になる**。✅ **「音声認識」として張り直し、2回目の押下は経路の切断で検知**（[adr/010](../adr/010_intercom_mic_routing.md)・[adr/008](../adr/008_end_of_speech_detection.md) 改訂、`pre-research/mic-routing/` §9・§10）。副産物: **ボタン押下の二重処理を修正**（`useURL()` が古いURLを返す）・**経路と録音の記録を端末内ファイルに残すように**。あわせて **エージェントに現在日時を渡すように**（Backend `prompt.py`。未来の日付と取り違える問題） |
| 2026-09-25 | **Agent・Backend・App・CICD を submodule に分離**（[adr/011](../adr/011_repository_split.md)）。リポジトリ作成・push・CICDスタックの入れ替えまで完了 |
| 2026-09-22 | **インカムのマイクで録れるように**（App **v1.36.0**・[adr/010](../adr/010_intercom_mic_routing.md)）。⚠️ **それまで本体マイクで録っていた**（`expo-audio` の `setInput()` が仕様違反で黙って失敗）。経路だけ自前モジュール `bt-audio-route` で張る |
| 2026-09-13 | 🎉 **Playの内部テスト経由で実機にアプリを配れた**（App **v1.35.0**）。**署名鍵の作成→AAB→Play Console登録→アップロード→インストール→動作確認**まで一巡。⚠️ **詰まったのは「テスターに『アイテムが見つかりませんでした』」で、原因は初回リリースの反映待ち**（設定は全て正しかった。**数時間で入った**）。📌 **内部テストに「国/地域」の設定は無い**（全世界が対象）ので疑っても無駄。一般知識は [learning/14](../learning/14_android_app_signing_and_release.md)、画面の迷いどころは `App/SETUP.md` §6 |
| 2026-09-10 | **Playに出せるビルド構成を実装**（App **v1.35.0**・[adr/009](../adr/009_play_internal_testing_release.md)）。⚠️ **debug鍵署名・`versionCode 1` 固定・arm64のみという「Playが受け付けない3点」を解消。** **`versionCode` は `version` から自動導出**（`1.35.0`→`13500`。`withVersionCode.js`）、**署名は環境変数から**（`withReleaseSigning.js`。⚠️ **`build.gradle` に平文で残さない**）、**ABIは `TRG_ALL_ABI` で切替**（配信時だけ全4種）。**`npm run bundle:play` で 72MB のAABを確認済み**（全4ABI・upload key署名を実測で検証）。⚠️ **`gradlew` は `.env` を読まない**と判明したのでスクリプトで明示的に読ませている（⚠️ **直接叩くと黙って debug 署名になる**）。⚠️ **独立レビューで4件の不具合が出たので修正済み**: ①**pluginが冪等でなく `--clean` なしのprebuildで署名ブロックが二重化**（⚠️ **`expo run:android` で日常的に踏む**）②⚠️ **`.env` のパスワードは引用符で囲まないと `#`・空白で空になる**（`SETUP.md` に明記）③`versionCode` の下限 ④パースの緩さ。📌 **訂正: upload key は失っても Play App Signing で再登録できる**（24〜48時間）。**回復不能なのはGoogleが預かる app signing key** |
| 2026-09-10 | **配信方針の調査**（[learning/13](../learning/13_private_app_distribution.md)）。⚠️ **無料の「限定配信」（20台・身分証不要）はPlayストアには使えない**と判明（Playの外で配る人向けの別製品）。**Playを使うなら $25 + 身分証**で、✅ **許容と決定。** 📌 **内部テストなら12人・14日のクローズドテストもData safetyフォームも不要。** ⚠️ **CI/CDは可能だが初回だけ手動が必須。** 副産物: **ABIを `arm64-v8a` のみに絞り 18.9GB→3.6GB**（v1.34.0・APKも108MB→45MB） |
| 2026-09-10 | **無音検知（VAD）を廃止し、画面を整理**（App **v1.32.0**・[adr/008](../adr/008_end_of_speech_detection.md) 改訂）。⚠️ **走行中に使えない機能のために仕様と設定画面が複雑になる**ため、**閾値・無音の長さ・猶予・音量計・dB表示・騒音ガードをまとめて削除**（`vadSettings.ts` → `recordingSettings.ts`。保存キーも変えて全員が既定値から始まる）。**「設定」「新しい会話を始める」を18pxのボタンに拡大**（⚠️ **停車中に押せないことがあった**）。**戻り先アプリを「戻る/戻らない」の二択＋アプリ選択に分離**（⚠️ **一時的に切ると選択ごと消えていた**）。**最近選んだアプリを上位に出し、名前で絞り込めるように**（お気に入りの手動登録は作らない — ★を付ける操作自体が「多すぎる一覧から探す」作業になるため） |
| 2026-09-10 | 🎉 **走行中の終話判定が解決。US-2.04がハンズフリーとして成立した**（[adr/008](../adr/008_end_of_speech_detection.md)・App **v1.31.0**）。**インカムのボタン再押し（C）＋録音の上限20秒（D）** の併用。⚠️ **録音中でもIntentが届くことを実機で確認**（Cを過去に却下した理由がこの未検証性だった）。📌 **Bは不要になった**（Transcribe経路を捨てずに済んだ）。⚠️ **無音検知と騒音ガードを既定で切った**ので**走行中も毎回送られる**（それまでは必ず捨てられていた）。**残り秒数を56pxで表示**（「押さずに待つ」判断のため）。📌 **副産物**: Hermesバイトコードは日本語がUTF-16でgrepが空振りする件を `App/SETUP.md` に記録 |
| 2026-09-09 | **終話判定を C+D（インカムのボタン再押し＋録音の上限）に変更**（[adr/008](../adr/008_end_of_speech_detection.md)・App **v1.30.0**）。⚠️ **録音中の `VOICE_COMMAND` を「送信」に分岐**（それまでは「いま応答中です」と読み上げていた）。**無音検知を設定で切れるようにし既定を「切」に**（⚠️ **切ると騒音ガードも一緒に止まる** — 走行中は必ず捨てるため）。`audioSource` の既定を **`voice_communication`** に（録音品質のため）。📌 **`FINDINGS.md` の節番号の重複（§12/§13が2組）を §14・§15 に振り直し**、参照元も追随。⚠️ **実機検証は未了**（手順は §16） |
| 2026-09-09 | ❌ **`audioSource` では飽和を回避できないと確定**（App v1.29.0・4種を実機で測り比べ。`FINDINGS.md` §15）。⚠️ **最良の `voice_communication` はアイドリング中なら成立した**（-50dB、発話で上昇）が、⚠️ **アクセルを開けると0dB付近に達する**ため走行では使えない。📌 **ノイズ除去は4種で最も効いたので、録音品質のためにこの設定は残す価値がある。** これで**音量ベースのVADは打ち止め**となり、方式の再検討へ |
| 2026-09-05 | ❌ **実走行で音量ベースのVADの破綻を確認**（`FINDINGS.md` §14）。⚠️ **エンジン始動で `metering` が 0 dBFS に飽和**し、発話しても値が動かない。⚠️ **閾値の調整・相対判定では原理的に直らない**（飽和＝情報が消えている）。📌 **原因は風切り音ではなくエンジン音**と判明し、**停車＋アイドリングで再現するので検証に走行が不要**になった。📌 **「全く機能しなかった」の正体は騒音ガードの正常な発動**でバグではない。副産物: `learning/12`（音・dB・VAD）新設、`learning/10` に3種のビルドの違いを追記 |
| 2026-08-22 | **`docs/` の監査と補完。** ⚠️ **アプリ側の設計がまるごと欠けていた**ので [docs/01c](../docs/01c_app_client.md) を新設（起動経路・VAD・マップ復帰・音声モード・単体ビルド）。⚠️ **`docs/00` 補足Cが却下済みの旧方式のまま**で「既定アシスタントに本アプリを選ぶ／Google Assistantを明け渡す」と誤記していたのを修正（**現行方式ではGoogleのままでよい**）。完了済みの未決事項も整理し、`docs/README` に `01b`/`01c` を追加 |
| 2026-08-22 | 🎉 **Metro無しで動く単体ビルド（release APK）が完成**（App **v1.27.0**・手順は `App/SETUP.md`）。**JSバンドルの埋め込みを確認済み**（debug APKには入っていなかった）。⚠️ **論点だった「releaseで `console.log` が出るか」は出ると確定**（除去の経路が全て不在）＝**走行中の `[vad]` ログが取れる**。署名はdebugと同じキーストアなので**上書きインストールで設定は消えない**。📌 **未実施: 実機へのインストールとUSBを抜いての起動確認**（ユーザーが実行する） |
| 2026-08-21 | 🎉 **応答後にマップアプリへ戻るようになった。実機で確認済み**（App **v1.22.0**・[adr/007](../adr/007_return_to_map_after_answer.md)）。**設定画面で選んだアプリをLAUNCHERインテントで開く**方式。⚠️ **当初の `moveTaskToBack` は不成立**（自分のタスクを下げるだけで、次に前面へ出るのは**たまたま下にあったもの**。ホームに落ちたり、たまたまマップに戻ったりと**不安定**）。⚠️ **他アプリのタスクは列挙も前面化もできない**（Android 5以降の制限）。📌 **「開き直すとナビが壊れる」という懸念は実測で覆った** — LAUNCHERインテントは**既存タスクの再開**でタスクIDが変わらない。⚠️ **既定が「戻らない」で黙って何もしないため、設定し忘れが「故障」に見えた**ので、未設定を画面に出すようにした |
| 2026-08-18 | 🎉 **US-2.04の「終了」側が実機で成立。ハンズフリーが一巡した**（App **v1.19.0**）。無音検知（VAD）で話し終えると自動送信。**騒音でマイクが埋まった場合は送らず端末内蔵TTSで通知**（`adr/006`）。閾値・無音の長さ・猶予・録音の上限を**設定画面から変更可能**にし、**リアルタイム音量計**も追加（AsyncStorage・expo-speech を追加＝要再ビルド）。⚠️ **`-160` は無音ではなく「測れなかった」を表す番兵**で、録音開始直後に必ず出る。1サンプルで騒音ガードが無効化される不具合を実機ログで特定し修正（`FINDINGS.md` §10〜11）。⚠️ **走行中の閾値は未検証** |
| 2026-08-17 | 🎉 **US-2.04（ハンズフリー起動）の起動側が実機で成立。本プロジェクト唯一の技術的リスクが解消**（`adr/006`・App v1.10.0）。⚠️ **当初想定した方式B（`VoiceInteractionService`）は実機で不成立と判明**し、`android.intent.action.VOICE_COMMAND` を `MainActivity` の intent-filter で受ける方式に変更（`App/plugins/withVoiceInteraction.js`）。副産物: JDK17/Android Studio/SDKのローカル環境構築（`learning/09`）、Expo Development Buildへの移行、`@expo/ui`（未使用）と`expo-asset`の重複依存を解消（マルチdexでクラス欠落を起こしていた）。**残課題: 録音の終了はまだ画面操作が必要**（`issues.md`） |
| 2026-08-16 | 🎉 **US-2.01/2.02（音声入出力）完了。実機で確認済み**（アプリ **v1.5.0**）。「押して話す」1ボタン（⚠️ **停止と送信を分けない**／30秒で自動送信）。回答は自動で読み上げ（**`Kazuha`**。⚠️ `Mizuki` はスタンダードのみで候補外）。⚠️ **`expo-audio` は Expo Go 同梱**なので Development Build は不要だった。ポーリングは80→120秒（音声は実測15〜20秒） |
| 2026-08-16 | **音声化のBackendを実装しデプロイ**（`POST /ask-audio` → S3 → Transcribeバッチ → EventBridge → 既存の非同期経路 → Polly → 署名付きURL）。テスト84→136件。⚠️ **まだ動かしていない**（アプリ未実装）。方式は `adr/002` を改訂し**STT/TTSをLambda側へ**（アプリ側で呼ぶ前提が誤りだった）。⚠️ **push前のレビューで4件の致命的欠陥**: `Decimal`（DynamoDBはfloat不可・かつ戻さないと住所が消える）・CI/CDの権限不足・`CAPABILITY_NAMED_IAM`。**既存の取り残し検出も同じ理由で動いていなかった** |
| 2026-08-15 | 🎉 **CI/CD（CodeBuild）が稼働。** pushで Agent → Backend を自動デプロイ（`adr/005`）。ARNの受け渡しはSSM（リージョンが違いCFnのエクスポートが使えない）。⚠️ 立ち上げで4回失敗した: ①CodeBuild用のGitHub認証情報の登録漏れ ②通知のサービスリンクドロール待ち ③パスフィルタがheadコミットしか見ず空振り（削除）④npmパッケージ名の誤り・テスト依存の未宣言。詳細は `CICD/README.md` |
| 2026-08-15 | ディレクトリ名を `App/` `Backend/` `Agent/` に統一し、`CICD/` を新設。`App/README.md` に起動手順を記載（それまで雛形のままだった） |
| 2026-08-15 | 🎉 **APIキー認証 + Usage Plan 完了。実機で確認済み**（`adr/004`）。スマホの設定画面からキーを入力して回答を得られた。IP制限は削除し、**モバイル回線からも叩ける**ようになった。`expo-secure-store` に保管。アプリ **v1.4.0** |
| 2026-08-13 | **セッション成果を別エージェントにレビューさせ、指摘を反映**（`processing` の取り残し回収・ポーリング中断時の無反応・節番号の参照切れ7箇所）。テスト79→84件 |
| 2026-08-13 | **29秒制約を解消（非同期ポーリング）。** SQS + DynamoDB + worker/result Lambda。実機で会話継続まで確認。⚠️ **むしろ速くなった**（初回12.5秒→6.4秒、ウォーム10.2秒→4.2秒。`max_tokens` を800にしてトークン上限への張り付きが解消したため） |
| 2026-08-13 | 🎉 **US-2.03（進行方位）完了。実機で方位が正しいことを確認**（アプリに方位磁針を表示・v1.2.0）。算出はLambda側、2点目はアプリが履歴から選ぶ（5m以上・30秒以内）。停車中・転回直後は方位なし。`geo.py` に欠けていたテストも追加（21→53件） |
| 2026-08-13 | **セッション成果をレビューし、設計文書の齟齬を解消**（`docs/01` が却下済みの音声方式を記述していた等）。`learning/61` に逆ジオコーディングの汎用知識を追加 |
| 2026-08-13 | `.memory/` を新設し、論点と作業の管理を `CLAUDE.md` から分離（30項目まで膨張していた進捗リストを解消） |
| 2026-08-12 | 🎉 **MVP（P0）完了。** `docs/00_user_stories.md` §6 の全項目を実機で確認 |
| 2026-08-12 | US-1.05（会話のリセット）を追加。ナビゲーションバーとの重なりも解消（`learning/08`） |
| 2026-08-12 | **座標→住所の精度問題を修正**（Amazon Location）。約40kmの誤答が解消し、回答も200→86文字に |
| 2026-08-12 | **US-1.01/1.03: アプリ→Lambda→AgentCore を接続**（`POST /ask` がモックを卒業） |
| 2026-08-12 | 音声方式の検証（`pre-research/voice/` に台本・スクリプト・実測値） |
| 2026-08-11 | **US-1.04: Web検索が成立**（AgentCore Gatewayの純正コネクタ） |
| 2026-08-11 | 要件定義を作成（`docs/00_user_stories.md`）。MVPスコープを確定 |
| 2026-07-30 | **US-1.02: 会話継続を実証**（AgentCore + `runtimeSessionId`） |
