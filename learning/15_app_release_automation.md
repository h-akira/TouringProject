# 15. アプリの配信を自動化する（ストアへの自動アップロード）

**「手元でビルドして、ストアの画面にドラッグする」を、コマンド（やCI）に任せる話。**
署名やAABそのものは [14](14_android_app_signing_and_release.md)、配信手段の選び方は [13](13_private_app_distribution.md)。
📌 **このプロジェクトが実際にどれを選んだかは §10。**

> **Webとの対応**: Webなら **S3に `aws s3 sync` すれば配信完了**。
> ⚠️ **アプリはストアという「他人のシステム」を経由する**ので、
> **そこにAPIで話しかける**ことになる。⚠️ **やることは増えるが、考え方は同じ。**

## 1. ⚠️ そもそも何を自動化するのか

**ストアへの提出は、突き詰めると3工程しかない。**

```
① ビルド        ソース → AAB（⚠️ 署名もここ）
② アップロード   AAB → ストアのサーバ
③ 公開          「テスターに配れ」と指示する
```

📌 **手でやると、②③がブラウザ作業になる。** ⚠️ **①も数分かかる。**
**自動化とは、この3つをコマンドに置き換えること。**

## 2. ストアのAPI

**Google Play には [Play Developer API](https://developers.google.com/android-publisher/getting_started) がある。**
⚠️ **AWSのAPIを叩くのと同じ**で、HTTPで操作する。

### ⚠️ 「Edit」というトランザクション

**ここが独特なので、先に理解しておくと迷わない。**

⚠️ **1回のアップロードは、単発のリクエストではない。**
**「編集セッション（edit）」を開いて、変更を積んで、最後にまとめて確定する。**

```
insert  → 編集セッションを開く（⚠️ この時点では本番に影響しない）
upload  → AABを送る、トラックを指定する、リリースノートを書く
commit  → ⚠️ ここで初めて全部が反映される
```

📌 **DBのトランザクションと同じ発想。**
⚠️ **「AABは新しいのに説明文が古い」といった中途半端な状態を防ぐ**ためにこうなっている。

⚠️ **commit しなければ何も起きない**ので、**途中で失敗しても壊れない。**

> 📌 **後述のツールは、どれもこの流れを代わりにやってくれるだけ。**
> ⚠️ **中で起きていることは全部同じ。**

### ⚠️ 初回だけは手で上げる必要がある

⚠️ **APIは「すでに存在するアプリ」しか更新できない。**
**パッケージ名がストアに登録されるまで、APIは相手にしてくれない。**

📌 **つまり「1回目は必ず手動、2回目から自動」**という順番になる。
⚠️ **これはツール選びの問題ではなく、APIの制約。**

## 3. 誰がAPIを叩くのか（サービスアカウント）

⚠️ **CIは「人間のアカウント」でログインできない**（パスワードも2段階認証も無理）。

📌 **そこで「プログラム用のアカウント」を作る。** これが**サービスアカウント**。

> **AWSでいうと**: 📌 **IAMロール**に近い。
> ⚠️ **人ではなくプログラムに権限を与える**という発想が同じ。

| | |
|---|---|
| **作る場所** | ⚠️ **Google Cloud**（⚠️ **ストアの管理画面とは別のサービス**） |
| **認証の材料** | **JSONファイル**（⚠️ **鍵そのもの。漏らせない**） |
| **権限の与え方** | ⚠️ **ストア側の管理画面で「ユーザー」として招待**する |

⚠️ **権限は絞る。** 📌 **「テスト用トラックへのアップロードだけ」にしておけば、
仮に漏れても本番公開はされない。**

⚠️ **作ってすぐには使えないことがある**（Google側の反映に時間がかかる）。

## 4. CI（GitHub Actions）とは

**「pushされたら、用意しておいた手順を自動で走らせる」仕組み。**

> **AWSでいうと**: 📌 **CodeBuild + CodePipeline** に相当する。
> ⚠️ **`buildspec.yml` が `.github/workflows/*.yml` になっただけ**と考えてよい。

| CodeBuild | GitHub Actions |
|---|---|
| `buildspec.yml` | `.github/workflows/<名前>.yml` |
| ビルド環境（イメージ） | **runner**（`ubuntu-latest` 等） |
| Parameter Store / Secrets Manager | ⚠️ **GitHub Secrets**（後述） |
| CloudWatch Logs | 画面上のログ |

**基本の形:**

```yaml
on:                       # ⚠️ いつ走るか
  push:
    tags: ['v*']          # タグがpushされたとき
jobs:
  release:
    runs-on: ubuntu-latest   # どこで走るか
    steps:
      - uses: actions/checkout@v4   # 既製の部品を使う
      - run: ./gradlew bundleRelease  # コマンドを実行する
```

### 📌 `uses` と `run` の違い

| | |
|---|---|
| **`run`** | ⚠️ **シェルコマンドをそのまま実行**する |
| **`uses`** | ⚠️ **他人が作った部品（Action）を呼ぶ。** 📌 **npmパッケージのようなもの** |

⚠️ **`uses` は他人のコードを実行する**ということ。**素性を確かめる必要がある**（§6）。

### ⚠️ Secrets は「書き込み専用」

**CIに秘密を渡す仕組み。** ⚠️ **ただしAWSとは決定的に違う点がある。**

| | Parameter Store | GitHub Secrets |
|---|---|---|
| **読み出し** | ✅ **CLIでいつでも読める** | ❌ **一度入れたら二度と読めない** |
| **バックアップになるか** | ✅ なる | ⚠️ **ならない** |

⚠️ **だから「Secretsに入れたから安心」ではない。** **原本は別に保管する。**

📌 **ログに出そうとすると自動で `***` にマスクされる**（⚠️ **ただし base64 を展開して出せば漏れる**）。

### 📌 Environment secrets —「誰が開けられるか」を絞る箱

**Secrets には置き場所が2つある。**

| | **Repository secrets** | **Environment secrets** |
|---|---|---|
| **読めるワークフロー** | ⚠️ **そのリポジトリのどのワークフローでも** | 📌 **`environment: <名前>` と書いたジョブだけ** |
| **読める条件を絞れるか** | ❌ | ✅ **「このブランチ・タグからの実行だけ」に制限できる** |

> **AWSでいうと**: 📌 **IAMポリシーの `Condition`** に近い。
> ⚠️ **「持っている」だけでなく「どこから来たか」で開けるかどうかが決まる。**

```yaml
jobs:
  release:
    environment: play   # ⚠️ この箱を開ける。条件を満たさない実行には Secret が渡らない
```

⚠️ **署名鍵のような重い秘密は Environment に入れ、配信のタグ（`v*`）からだけ読めるようにする**のが定石。
⚠️ **規則は「ブランチ」か「タグ」かを選んで作る（既定はブランチ）。**
**`v*` をブランチの規則として作ると「`v` で始まるブランチ」の意味になり、タグからの実行は箱を開けられない。**
⚠️ **private リポジトリでは無料プランで Environment を使えない**（public なら使える）。

### ⚠️ 手元の `.env` は CI には無い

**手元のビルドは、gitignore した `.env` から設定を読んでいることが多い。**
⚠️ **CI はリポジトリをクローンするだけなので、`.env` は存在しない。**

⚠️ **特に危ないのは「ビルド時に焼き込まれる値」**（Expo の `EXPO_PUBLIC_*`、Vite の `VITE_*` 等）。

> **Webとの対応**: 📌 **SPA のビルドで API の URL を環境変数から埋め込むのと同じ。**
> ⚠️ **渡し忘れても、ビルドはエラーにならず「空の値」で成功する。**

📌 **つまり「ビルドも署名も通るのに、動かないアプリが配られる」。**
**CI に渡す値は、手元の `.env` の中身と1つずつ照らし合わせる**。さらに**無ければビルドを止める**（§8）。

## 5. ⚠️ 署名鍵をCIに置くかどうか

⚠️ **自動化で必ずぶつかる分岐。**

**①のビルドをCIでやるなら、⚠️ CIが署名しなければならない＝鍵をCIに渡すことになる。**

| | **CIでビルド・署名** | **手元でビルドし、AABだけ上げる** |
|---|---|---|
| **push だけで完結** | ✅ **する** | ❌ **しない**（手元作業が残る） |
| ⚠️ **鍵の置き場** | ⚠️ **CIにもコピーが存在する** | ✅ **手元だけ** |

⚠️ **鍵はバイナリなので、Secretsに入れるには base64 で文字列化する。**

### 📌 リスクの見積もり方

⚠️ **「漏れたら何が起きるか」で考える。**

- ⚠️ **第三者が「そのアプリの更新」として署名できる**ようになる
- 📌 **ただしストアに上げるにはサービスアカウントの鍵も要る**（二重の壁）
- ✅ **upload key なら再登録できる**（[14](14_android_app_signing_and_release.md) §3）

⚠️ **publicリポジトリでは特に注意。** 📌 **フォークからのPRでワークフローが走る設定にすると、
他人がSecretsに触れる経路ができうる。**

### 📌 鍵は使い終わったらすぐ消す

**CI の中では、base64 から戻した鍵が一時的にファイルとして存在する。**
⚠️ **その後に他人の Action（§6）を実行するなら、その前に消しておく。**
📌 **途中のステップで失敗しても消えるように、`if: always()` の後始末も置く。**

```yaml
- run: rm -f "$RUNNER_TEMP/upload.jks"      # 他人の Action の前に消す
- uses: someone/upload-action@<SHA>
- if: always()                              # 途中で失敗しても必ず消す
  run: rm -f "$RUNNER_TEMP/upload.jks"
```

## 6. アップロードの手段（3通り）

⚠️ **どれを選んでも、§2のAPIを叩いている点は同じ。** **違いは「誰が書いたコードを使うか」。**

| | **既製のCLIツール** | **既製のCI部品（Action）** | **自分で書く** |
|---|---|---|---|
| **代表例** | **fastlane**（Ruby製・モバイル開発の定番） | `r0adkll/upload-google-play` 等 | Python/Node のスクリプト |
| **書く量** | ✅ **設定ファイルだけ** | ✅ **YAMLに数行** | ⚠️ **数十行** |
| **依存** | ⚠️ **Ruby環境が要る** | ⚠️ **他人のビルド済みコードを実行** | ✅ **公式ライブラリのみ** |
| **中身の把握** | ⚠️ 難しい | ⚠️ **難しい**（ビルド済みJSが実行される） | ✅ **完全に把握できる** |

> **AWSでいうと**: 📌 **AWS CLI を使うか / 誰かのCloudFormationテンプレートを使うか /
> boto3 で自分で書くか**、の三択に近い。

### ⚠️ 既製品を使うときの注意

⚠️ **`uses: someone/action@v1` のような可変タグは、あとから中身を差し替えられる。**

📌 **コミットSHAで固定すれば、その時点のコードに釘付けにできる:**

```yaml
- uses: someone/action@e738b9dd8f2476ea806d921b64aacd24f34515a5
```

⚠️ **個人が管理するリポジトリを使うなら、これは必須と考えてよい。**
📌 **公式の Action（`actions/checkout` 等）もSHAで固定しておけば、例外を考えずに済む**
（GitHub のセキュリティガイドもSHAでの固定を勧めている）。
SHAだけだと版が読めないので、**行末に `# v4.2.2` のように注記する**のが慣習。
⚠️ **代わりに、SHAで固定した Action には Dependabot の脆弱性アラートが出ない。** **版は自分で定期的に上げる。**

### 📌 ワークフローの権限も絞る

**ワークフローには `GITHUB_TOKEN` という、そのリポジトリを操作できるトークンが自動で渡される。**
⚠️ **既定の権限は設定によっては広い**ので、**必要な分だけ宣言する:**

```yaml
permissions:
  contents: read   # ソースを読むだけ。ストアへのアップロードには GitHub の権限は要らない
```

> **AWSでいうと**: 📌 **CodeBuild のサービスロールを最小権限にする**のと同じ。

## 7. 何をきっかけに走らせるか

| きっかけ | 評価 |
|---|---|
| **push すべて** | ⚠️ **ドキュメント修正でも配信が走る。** ⚠️ **バージョンが同じだとストアに弾かれ、失敗通知が常態化する** |
| ⚠️ **タグ（`v1.2.3` 等）** | ✅ **「バージョンを上げた」という意思表示と一致する**ので相性がよい |
| **手動実行** | 📌 **確実だが、押しに行く手間は残る** |
| **特定パスの変更を検知** | ⚠️ **CIによっては最新コミットしか見ない**ので、取りこぼす罠がある |

📌 **バージョン番号を上げないと、そもそもストアが受け付けない**（[14](14_android_app_signing_and_release.md) §5）。
⚠️ **つまり「配信してよいタイミング」は、バージョンを上げた瞬間と一致している。**

## 8. ⚠️ 上げる前に、成果物を機械に検査させる

**手作業なら、アップロードの前に人が何となく確かめていた。**
⚠️ **自動化すると、その目が無くなる。** 📌 **代わりに「おかしければ止まる」検査をワークフローに書く。**

| 検査 | 防ぐ事故 |
|---|---|
| **タグとアプリの `version` が一致するか** | ⚠️ **違う番号でビルドして、ストアに弾かれる／番号を無駄に消費する** |
| **debug鍵で署名されていないか** | ⚠️ **鍵の設定漏れ**（黙って debug 署名にフォールバックするビルドがある） |
| **対応するCPU（ABI）が揃っているか** | ⚠️ **開発用に絞った設定のまま配る**（[14](14_android_app_signing_and_release.md)） |
| **ビルド時に埋め込む値が入っているか** | ⚠️ **§4 の「`.env` が無い」事故** |

📌 **ポイントは「ビルドが成功したか」ではなく「中身が正しいか」を見ること。**
⚠️ **上の事故は、どれもビルド自体は成功する。**

> **Webとの対応**: 📌 **デプロイ後のスモークテスト**に近い。
> ⚠️ **ただしストアは一度上げた番号を取り消せない**ので、**上げる前に止める**方が重要になる。

## 9. 📌 自動化する前に考えること

⚠️ **自動化は「手が空く」代わりに「持ち物が増える」。**
⚠️ **「速くなる」とは限らない。** 📌 **CIのマシンは手元より非力なことが多く、キャッシュも無い。**
**手元で数分のビルドが、CIでは20分を超えることもある。**
**得られるのは速さではなく、①待っている間に手が空く ②毎回同じ手順で作られる、の2つ。**

| 増えるもの | |
|---|---|
| ⚠️ **鍵の置き場** | 手元だけだったものが、CIにも増える |
| ⚠️ **壊れる箇所** | ⚠️ **CIの環境（SDK/JDKのバージョン）が手元とずれると、CIだけ失敗する** |
| **設定ファイル** | ワークフロー・サービスアカウント・権限 |

📌 **配る頻度が低いなら、手動のままでも十分**なことはある。
⚠️ **逆に「直して、すぐ配って、また試す」という往復があるなら、自動化の価値は大きい。**

📌 **判断の軸は「1回あたりの手間 × 回数」が「作る手間 + 抱えるリスク」を超えるか。**

## 10. 📌 このプロジェクトでは

⚠️ **ここまでの一般論に対して、このプロジェクトが選んだもの。**
**判断の経緯は [adr/009](../adr/009_play_internal_testing_release.md) の改訂節**、実物は App の
[play-release.yml](https://github.com/h-akira/TouringProject_App/blob/main/.github/workflows/play-release.yml)。

| 論点 | 選んだもの | 本書の節 |
|---|---|---|
| **初回** | 手動で上げた（APIの制約） | §2 |
| **署名** | ⚠️ **CIで署名する。** upload key の写しを **Environment secrets** に置き、**`v*` タグからだけ**読める | §4・§5 |
| **アップロードの手段** | **既製の Action**（`r0adkll/upload-google-play`）。**公式の Action も含めて全部SHA固定** | §6 |
| **きっかけ** | **`v*` タグの push**（もともとあった「`version` を上げたらタグを打つ」規約に乗せた） | §7 |
| **検査** | §8 の表の4つを全部やっている | §8 |
| **CIの基盤** | ⚠️ **GitHub Actions。** バックエンドの CodeBuild には入れない（アプリ側をAWSに依存させない） | §4 |

📌 **やってみて分かったこと:**

- ⚠️ **ビルド時に埋め込む API の URL を CI に渡し忘れていた**（§4）。**ビルドも署名の検査も通っていた**ので、**独立したレビューで初めて気づいた。** → §8 の検査を足した。
- ⚠️ **Environment の規則をタグではなくブランチとして作ってしまい、タグからの実行で Secret が読めなかった**（§4）。
- 📌 **CIでのビルドは約22分**（手元は4〜5分）。§9 のとおり、**速くはならない。**

## 参考

- [Google Play Developer API - Getting Started](https://developers.google.com/android-publisher/getting_started)
- [Edits（トランザクションの仕組み）](https://developers.google.com/android-publisher/edits)
- [GitHub Actions のドキュメント](https://docs.github.com/actions)
- [GitHub Actions - Deployments and environments](https://docs.github.com/en/actions/reference/workflows-and-actions/deployments-and-environments)（Environment の Secret とブランチ・タグの制限）
- [GitHub Actions - Secure use reference](https://docs.github.com/en/actions/reference/security/secure-use)（SHA での固定・`GITHUB_TOKEN` の最小権限）
- [fastlane](https://docs.fastlane.tools/)
