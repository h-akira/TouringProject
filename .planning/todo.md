# TODO — やること と、やり終えたこと

> **このファイルは「確定していること」を持つ。**
>
> | 節 | 中身 |
> |---|---|
> | 次にやる | **やる順番が決まっている作業。** 上から着手する |
> | 完了したもの | **やり終えたこと。** 日付つき・消さない。**決めた結論もここに書く** |
>
> 時期が未定のもの・まだ決めていない論点は [issues.md](issues.md) へ。
>
> 📌 **論点も同じ流れをたどる。** `issues.md` の「Xをどうするか」→ 着手が決まって
> 「Xを決める」→ 完了時に**結論をここに残す**。だから結論の置き場を別に設けない。
>
> **このファイルはAIが更新する**（人は読むだけ）。書き方と更新タイミングは `CLAUDE.md`。
>
> 最終更新: 2026-08-13

## いま取り組んでいること

**なし**（MVP完了直後。次は下記「次にやる」から着手する）

## 次にやる

### 1. US-2.03: 進行方位の算出

**なぜ先か**: 「右手に見える山は？」という**コア体験そのもの**で、
かつ**周辺検索の作り込みの前提**（方角が分からないと「右手」を判定できない）。

- `watchPositionAsync` で連続取得し、2点間から方位を算出する
- 算出は**アプリ側かLambda側か**を決める（座標の扱いはLambdaに寄せた前例がある）
- `AskRequest` の `end`（すでに任意で定義済み）を実際に使う

### 2. 音声化（US-2.01 / US-2.02）

方式は決定済み（**手前でSTT** = アプリでTranscribe → テキスト送信 → Pollyで読み上げ）。
検証済みの資産が `pre-research/voice/` にある（台本・スクリプト・実測値）。

- ⚠️ **回答の長さを先に詰める**。読み上げると長すぎる問題は住所修正で改善したが、
  方位が絡む質問では再発しうる
- ⚠️ 実装前に **29秒制約**（[issues.md](issues.md)）の結論を出す。
  同期呼び出しのままだと音声化で詰まる可能性がある

## 完了したもの

> **消さずに残す。** この粒度なら量は増えないので、到達点の記録として使う。
> **結論が出た論点もここに書く**（「何を決めたか」は「何をやったか」の一種）。
> 新しいものを上に足す。

| 完了日 | やったこと | 結論・成果 |
|---|---|---|
| 2026-08-13 | `.planning/` を新設し、進捗管理を `CLAUDE.md` から分離 | 30項目まで膨張していた進捗リストを解消 |
| 2026-08-12 | 🎉 **MVP（P0）完了** | `docs/00_user_stories.md` §6 の全項目を実機で確認 |
| 2026-08-12 | US-1.05（会話のリセット）を追加 | ナビゲーションバーとの重なりも解消（`learning/08`） |
| 2026-08-12 | **座標→住所の精度問題**を調査・修正 | **Lambdaで Amazon Location を呼ぶ**（`@tool` にしない。毎回必要な前提情報なので判断させない）。約40kmの誤答が解消し、回答も200→86文字に（`pre-research/geocoding/` §3） |
| 2026-08-12 | **US-1.01/1.03: アプリ→Lambda→AgentCore を接続** | `POST /ask` がモックを卒業。**Lambdaは挟む**と確定（門番＋事実の確定という役割） |
| 2026-08-12 | **音声の実現方式を検討** | **方式1（手前でSTT）を採用。** Nova 2 Sonicは**日本語非対応**で不採用（`pre-research/voice/` §1） |
| 2026-08-12 | 初回応答が遅い原因を調査 | **コールドスタートと判明。許容する**（アプリ側では縮められない。定常は2〜3秒。`pre-research/voice/` §6） |
| 2026-08-11 | **US-1.04: Web検索が成立** | AgentCore Gatewayの純正コネクタを採用（`pre-research/websearch/`） |
| 2026-08-11 | 要件定義を作成 | `docs/00_user_stories.md`。MVPスコープを確定 |
| 2026-07-30 | **US-1.02: 会話継続を実証** | AgentCore + `runtimeSessionId`。**AgentCore Memoryは採用しない**（要件は「一問一答＋α」まで） |
