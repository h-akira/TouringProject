# ⚠️ 録音そのものはコミットしない。
#
# 走行中の録音には**開発者本人の居場所と声**が入る（CLAUDE.md「公開リポジトリの鉄則」）。
# このリポジトリは public なので、一度コミットすれば履歴に永久に残る。
#
# 📌 **分かったことは FINDINGS.md に「文章と数値で」書く。** 音そのものは要らない。
samples/
env/

# 検証スクリプトが吐く中間ファイル
*.wav
*.m4a
*.flac
*.ogg

# ⚠️ **第三者の学習済みモデルは同梱しない**（RNNoise の *.rnnn）。
#
# **再取得できるもの**をリポジトリに持つ理由がなく、⚠️ **配布条件も別途のもの**になる。
# 📌 **取得手順は SETUP.md。**
models/
