# CLAUDE.md

プロジェクトの概要・構成は [README.md](README.md)。
共通規約は `~/.claude/CLAUDE.md` にあるので、ここには**本プロジェクト固有のもの**だけ書く。

> ⚠️ **前提**: バイク走行中に音声でAIと対話するアプリ。**走行中は画面を見られず手も使えない。**
> 設計で迷ったらこの制約に立ち返る。**学習教材も兼ねる**ので、判断の過程を残しながら進める。

## 技術選定と方針

**選定の一覧は [README.md](README.md)、理由は `pre-research/02` と `docs/01`。**
ここでは**判断に効くもの**だけ:

- **Web(PWA)は却下済み**（常駐＋完全ハンズフリーがブラウザでは不可能）。**前提を覆さない。**
- **ハンズフリー起動（US-2.04）は実機で成立済み**（本プロジェクト唯一の技術的リスクだった）。
  ⚠️ **方式は `android.intent.action.VOICE_COMMAND` を `MainActivity` の intent-filter で受ける**
  （`App/plugins/withVoiceInteraction.js`。[adr/006](adr/006_handsfree_launch_mechanism.md)）。
  ⚠️ **`VoiceInteractionService` は実機で不成立と判明し却下した。** 蒸し返さないこと。
  **ウェイクワードも却下**（常時マイク占有）。
- **IaCは `Backend/`=SAM、`Agent/`=CDK**（`agentcore` CLIの仕様）。混同しない。

### 実装の重心

- **AWS側（Lambda/Bedrock等）は作り込んでよい** — 得意領域かつ価値の中核。
- **モバイル（RN側）は必要最低限** — "薄いクライアント"に徹する。
  UIは動作確認に足る最小限、凝った作り込みはしない。
- 迷ったら「その作り込みは**コア体験（走りながら音声で会話）に必要か**？」を基準にする。
## ドキュメントの書き分け（⚠️ 混同しやすい）

**各ディレクトリの役割は [README.md](README.md)。** ここには**書くときのルール**を置く。

| どこに | 何を書くか | 禁止 |
|---|---|---|
| `docs/` | **現在の設計だけ** | ⚠️ **履歴を書かない**（「当初は」「Aを却下して」） |
| `adr/` | 経緯・却下案。`NNN_*.md`（3桁連番） | ⚠️ **判断の中身を後知恵で塗り替えない**（下記）。**番号は再利用しない** |
| `pre-research/` | **検証で分かった事実**（実測値・スクリプト） | ⚠️ **判断を書かない**（それは `adr/`） |
| `learning/` | **学習教材。** 基礎を丁寧に、既存のWeb知識と対応づけて書く。**01〜49=モバイル/フロント、51〜59=AI、61〜99=AI以外のバックエンド** | — |

- ⚠️ **`learning/` と `docs/` は内容が重複してよい**（責務が違う）。
  同じ話題でも、`learning/` は**技術そのものを学ぶため**、`docs/` は
  **このアプリがどう作られているか**を示すために書く。
  例: 逆ジオコーディングは `learning/61`（一般的な知識）と
  `pre-research/geocoding/`（このアプリでの検証）の両方にある。
- **`docs/01` が肥大したら `01a` `01b` … に分ける**（全体像は `01` に残し、詳細を切り出す）。
- **非公開ファイル**: `pre-research/XX_*.md`（個人的メモ）は `.gitignore` で除外。

### ⚠️ `README.md` を肥大させない（どのディレクトリでも）

**`README.md` は「入口」であって、詳細の置き場ではない。**
⚠️ **書くのは①結論 ②ファイルの一覧 ③次の一手**まで。**根拠や経緯は別ファイルへ出す。**

| 目安 | やること |
|---|---|
| **150行を超えたら** | ⚠️ **分割を検討する** |
| **250行を超えたら** | ⚠️ **必ず分割する** |

**調査ディレクトリ（`pre-research/*/`）の分け方:**

| ファイル | 何を書くか |
|---|---|
| `README.md` | **結論・ファイル一覧・次の一手**（⚠️ **これだけ**） |
| `DESIGN.md` / `DECISION.md` | **なぜそうしたか**（設計判断・却下した案とその理由） |
| `FINDINGS.md` | **何が分かったか**（実測値・一次情報の引用） |
| `SETUP.md` | **どう作るか**（手順・ハマりどころ） |

- ⚠️ **「1ファイルに全部あった方が読みやすい」は成り立たない。**
  **読み手は普通『結論』しか要らない**ので、**根拠を同居させると結論が埋もれる。**
- 📌 **分割したら `README.md` に必ずファイル一覧の表を置く**（迷子にしない）。
- ⚠️ **節番号（§1.2 等）で相互参照するときは、分割時にリンクを貼り直す。**

### ADRの書き方（`adr/`）

`adr/NNN_<短い題>.md`（3桁の連番）。**番号は再利用しない**（却下された決定も番号を空けたまま残す）。
1ページに収め、詳細な設計は `docs/`、詳細な検証は `pre-research/` へ置いてリンクする。

```markdown
# NNN. <決定の題>

- **日付**: YYYY-MM-DD
- **ステータス**: 採用 / 却下 / 保留 / 採用（一部改訂） / ❌ 覆った（NNN で置き換え）

## 背景
## 選択肢    ← 却下したものも書く
## 決定
## 影響
```

⚠️ **`adr/` は「現在の設計」ではない。** 現在の設計は `docs/`。
ADRは**その時点で何を知った上でどう判断したか**の記録なので、**過去形として読む。**

#### 決定が覆ったとき

**「背景・選択肢・決定」を後知恵で書き換えない。** 判断の履歴が消えると同じ議論を繰り返す。
⚠️ 特に「**なぜ却下したのか**」は、次に同じ間違いを防ぐ情報なので必ず残す。

**覆りの規模で使い分ける**（判断の目安は「**背景と選択肢をそのまま使えるか**」）:

| 規模 | やり方 |
|---|---|
| **小** — 前提の一部が誤っていた等で、**決定の骨格は生きている** | **同じADRに「改訂」節を追記。** 日付・何が覆ったか・なぜかを書く。ステータスを `採用（一部改訂）` に |
| **大** — 決定そのものが別物になり、**背景から組み直しになる** | **新しいADRを書く。** 古い方は冒頭で無効を宣言して繋ぐ（下記） |

大きく覆った場合、古いADRの**先頭**に読み飛ばせない形で置く（AIが冒頭で誤認しないため）:

```markdown
> ⚠️ **この決定は覆った。[adr/NNN](NNN_*.md) を読むこと。**
> 以下は YYYY-MM-DD 時点の判断の記録であり、**現在の設計ではない。**
```

📌 **誤字・リンク切れ・参照先の節番号の追随・ステータス更新は修正してよい**（判断の中身ではないため）。

### ユーザーストーリーの書き方（`docs/00`）

- 「**〜として、〜したい。〜だから**」の形で**誰の・何を・なぜ**を書く。
- **IDは `US-<優先度>.<2桁連番>`**（例 `US-1.02`）。P0=1 / P1=2、**P2は未定なので `X`**。
  ⚠️ **連番を詰め直さない**（他ドキュメントからの参照が壊れる）。
- 「複数ユーザー対応」のような**実装側の都合はストーリーではない**（「やらないこと」へ）。
- **現在の姿**を示す。変更履歴的な記述は残さない（経緯はコミットメッセージへ）。
- 本文は要件に絞り、**理由・実装方針・実測データは末尾の「補足A/B…」へ**。
## 開発環境（Mac + 実機Android + Expo Development Build）

- ⚠️ **US-2.04の実装（Development Build）を機に、Expo Go から移行済み。**
  動作確認は **実機Android + Development Build**: `cd App && npx expo start` で
  Metro（開発サーバー）を起動し、実機にインストール済みのアプリ（`com.touringproject.app`）を開く。
  ⚠️ **`expo start` は対話型TUIなのでAIがバックグラウンド実行しない。**
  ユーザー自身のターミナルで起動してもらう。**手順は [App/SETUP.md](App/SETUP.md)**
  （初回セットアップ・中断/再開・つまずいたとき）。技術的な背景を学ぶなら
  [learning/09](learning/09_android_dev_environment.md)・[learning/10](learning/10_expo_development_build.md)。
- **ネイティブコード（`android/`）を変更したら `npx expo run:android` で再ビルド**が要る
  （JS/TSだけの変更は再起動不要でMetro経由で反映される）。
  ハンズフリー起動（US-2.04）は `App/plugins/withVoiceInteraction.js` という
  Expo config plugin で実装している（`VoiceInteractionService` ではなく、
  `android.intent.action.VOICE_COMMAND` の intent-filter。[adr/006](adr/006_handsfree_launch_mechanism.md)）。
- ⚠️ **`App/` を変更したら `App/app.json` の `version` を必ず上げる。**
  実機の画面左上に表示され、**更新が反映されたかの判別に使う**
  （キャッシュが残ることがあるため）。**上げ忘れると判別できない。**
- ⚠️ **`version` を上げたら、コミット時に注釈付きタグを打つ**（`git tag -a v<version>`）。
  ⚠️ **タグは App リポジトリ（`App/` の中）で打つ。** 親リポジトリには打たない
  （分離前の `v1.36.0` までは親にある）。
  ⚠️ **軽量タグ（`-a` 無し）は使わない** — 日付も作者も理由も残らない。
  📌 **タグ名は `v` + `app.json` の値**（`v1.31.0`）。**タグと version は1対1。**
  **タグのメッセージは英語**（コミットメッセージと同じ）で、
  ⚠️ **「何が動くようになったか」を書く**（差分の要約ではない）。
  ⚠️ **`v*` タグの push は Play の内部テストへの配信を意味する**（App の `.github/workflows/play-release.yml`。[adr/009](adr/009_play_internal_testing_release.md) 改訂）。
  📌 **`git push` と同じくタグの push もユーザーが行う**（`git -C App push --tags`。⚠️ **親で `git push --tags` しても App のタグは上がらない**）。

## ⚠️ リポジトリ構成（submodule）

**`Agent/` `Backend/` `App/` `CICD/` は submodule**（`TouringProject_<名前>`。⚠️ **CICD だけ private**、他は public。
[adr/011](adr/011_repository_split.md)）。**作業場所は基本的にこの親リポジトリ**で、
**サブに `CLAUDE.md` は置かない**（この1枚を読む前提）。

- ⚠️ **サブの変更は、サブの中でコミットする。** そのあと**親でポインタを更新するコミット**が要る
  （`git add <サブのディレクトリ>`。📌 **メッセージは `chore: bump <サブ> submodule`**）。
- ⚠️ **push はサブ → 親の順**（ユーザーが行う）。逆だと親が**存在しないコミットを指す**。
  `git push --recurse-submodules=check` で検出できる。
- 📌 **親のポインタはデプロイと無関係**（引き金は各サブの `main`）。
  **「ドキュメントがどの版を前提に書かれたか」の記録**なので、区切りのよいところで更新すればよい。
- ⚠️ **`.gitignore` はリポジトリごとに別。** 親の `.gitignore` は submodule の中には効かない。
- ⚠️ **サブの README から親へのリンクは絶対URL**（GitHub 上で単体表示しても切れないように）。
- ⚠️ **CICD は非公開。公開側（`README.md`・`docs/`・サブの README）では CI/CD の詳細を書かない**
  （不正利用の被害を避けるため。[adr/011](adr/011_repository_split.md) 改訂）。
  📌 **CI/CD があること・何を使っているか（CodeBuild）・CICD へのリンクは書いてよい**
  （他の人にはリンク切れに見えるが許容する）。⚠️ **構成・権限・順序の仕組みは書かない。**
  📌 **`CLAUDE.md` と `.memory/` には必要な範囲で書いてよい。** ⚠️ **CICD の中身も公開前提で書く**（秘密を入れない規約はそのまま）。
  📌 **対象は AI（Bedrock）を呼ぶ AWS 側（Agent・Backend）の CI/CD だけ。** 各リポジトリの `buildspec.yml` が公開されている程度は問題ない。
  ✅ **App の Play 配信（GitHub Actions）は対象外**なので、ワークフローも ADR も普通に書いてよい。
  📌 **ただし設定手順（Google Cloud・Play Console・GitHub の Environment）は CI/CD の一部として `CICD/PLAY_RELEASE.md` に置く。**

## AWS / バックエンド開発

- ⚠️ **リージョンが用途で分かれている**（混同すると動かない）:
  `Backend/`（SAM）= **東京**、`Agent/`（AgentCore）= **us-east-1**。
- **リソース命名規約**: `<リソースタイプ>-trg-<env>-<識別子>`
  （`trg`=touring、`env`=`dev`/`prod`、識別子は単一なら `main`）。
  例: `stack-trg-dev-main` / `lambda-trg-dev-ask` / `dynamodb-trg-dev-main`
- ⚠️ **デプロイと実機確認はユーザーが実行する。** AIは実行しない。
  手順・必要な権限は [Backend/README.md](Backend/README.md)。
- ⚠️ **AWS認証情報・アカウントIDは絶対に含めない**（`samconfig.toml` にも）。詳細は下記。

### CI/CD（pushで自動デプロイ）

**Agent・Backend の各リポジトリの `main` にpushすると、それぞれの CodeBuild がデプロイする**
（[CICD/](CICD/)）。**ブランチでの環境分け・CodePipeline・手動承認は使わない。**

- ⚠️ **Agent が Runtime ARN を SSM に書き、Backend の SAM がデプロイ時に読む。**
  **リージョンが違うのでCFnのエクスポートは使えない。**
  ⚠️ **ARN が変わったときだけ Agent のビルドが Backend のビルドを起動する**
  （Backend は ARN を焼き込むので、再デプロイしないと古いランタイムを呼び続ける）。
- ⚠️ **手順書は各リポジトリ直下の `buildspec.yml`。** `CICD/` にあるのは
  CodeBuild自体を作るCloudFormation。
- ⚠️ **`main` へのpushは常にビルドする。** パスフィルタは**使わない**:
  `FILE_PATH` は**headコミットしか見ない**ので、最後のコミットがドキュメントだけだと
  **同じpushのコード変更ごと静かにスキップされる**（`CICD/README.md`）。

## ⚠️ 公開リポジトリの鉄則（最重要）

**このリポジトリは public**（⚠️ **サブも CICD 以外は public**）。一度コミット・pushしたものはgit履歴に永久に残り、削除しても復元可能。
**コミット前に必ず確認すること。**

### 書いてはいけないもの

| 種別 | 例 | 代わりに書く |
|---|---|---|
| **AWSアカウントID（12桁）** | `123456789012` | `<ACCOUNT_ID>` |
| **組織ID / Root ID / OU-ID / SCP-ID** | `o-xxxx` `r-xxxx` `ou-xxxx` `p-xxxx` | `<ORG_ID>` 等 |
| **ARN**（アカウントID部分を含む） | `arn:aws:iam::123456789012:role/x` | `arn:aws:iam::<ACCOUNT_ID>:role/x` |
| **アクセスキー/シークレット** | `AKIA...` `ASIA...` | 論外。環境変数へ |
| **APIキー・トークン** | Bedrock/GitHub等のキー | 論外。`.env`（gitignore済）へ |
| **APIの実URL** | `https://abc123xyz0.execute-api...` | `https://<api-id>.execute-api...` |
| **IAMユーザー名・実在ユーザー名** | — | 役割名で表現 |
| ⚠️ **ローカルのユーザー名・ホームの絶対パス** | `/Users/<名前>/...`、⚠️ **ユーザー名と同じAWSプロファイル名** | **リポジトリからの相対パス**・`<mgmt-profile>` 等。⚠️ **GitHubのアカウント名（`h-akira`）とは別物** |
| **個人のメールアドレス** | — | 書かない |
| **実際の現在地の座標・住所** | 自宅や職場付近の緯度経度 | **東京駅などの公共ランドマークで代用** |

> ⚠️ **位置情報アプリ特有の注意。** 実機テストのログやAPI応答には**開発者本人の居場所**が入る。
> ドキュメントに書くときは**公共の座標で再現し直す**か、「約40km離れた市を答えた」のように
> **相対的な表現**にすれば事象は正確に記録できる（例: `pre-research/geocoding/`）。
>
> AWSアカウントIDはパスワードではないが、**標的の特定に使われ得る**ため公開しない。

### 一方で、書いてよいもの

- **AWSプロファイル名**（`touring` 等）— ローカルの設定名にすぎず秘密ではない
  （⚠️ **ただしローカルのユーザー名と同じ名前のプロファイルは書かない**）
- リージョン名、サービス名、モデルID、リソース**命名規約**（実IDでなければ可）
- 検証で得たレイテンシ・トークン数などの実測値

### コミット前チェック

```sh
# アカウントID(12桁)・組織/Root/SCP-ID・アクセスキーの混入検査
grep -rnE '\b[0-9]{12}\b|\b(o-[a-z0-9]{10,}|r-[a-z0-9]{4,}|p-[a-z0-9]{8,})\b|AKIA[0-9A-Z]{16}' \
  --include='*.md' --include='*.sh' --include='*.yaml' --include='*.py' \
  --include='*.toml' --include='*.ts' --include='*.tsx' --include='*.json' . \
  | grep -v node_modules
# ローカルのユーザー名・ホームの絶対パスの混入検査（⚠️ 追跡ファイルだけを見る。サブも含む）
# ⚠️ 名前そのものをここに書くと、それ自体が混入になるので whoami で引く
git grep -nI --recurse-submodules -e "$(whoami)" -e '/Users/[A-Za-z]' \
  | grep -vE '/Users/(you|<)'
# → 何も出なければOK
```

- ⚠️ **検証コマンドの出力を貼るときは特に注意**（ARNやアカウントIDが混入しやすい）。
- push済みで混入が判明したら、**まず該当リソースの無効化・ローテーション**
  （履歴書き換えだけでは不十分）。
- ⚠️ **既存のファイルを別リポジトリへ移す・コピーするときも検査する**
  （サブへの分離で、古いコメントがそのまま持ち込まれた）。

## API契約（OpenAPI）と型生成

- **`docs/02_api_openapi.yaml` が正本**（フロント↔バックの契約）。
  API Gatewayの `DefinitionBody` には未組込（契約・型生成・ドキュメント用途）。
- ⚠️ **App は写し（`App/src/api/openapi.yaml`）から型を生成する**（App を単体でクローンしても動くように）。
  ⚠️ **契約を変えたら `./scripts/sync-openapi.sh` で写しを更新し、App 側でもコミットする**
  （手動。⚠️ **忘れると App は古い契約のまま**）。**写しは手で編集しない。**
- ⚠️ **契約を変えたら型を再生成する**: `cd App && npm run gen:api`
  → `App/src/api/schema.ts`。エイリアスは `src/api/types.ts`。
- ⚠️ **`schema.ts` は生成物なので追跡しない**（`.gitignore` 済み）。
  **手で編集しない。** 直すのは正本の `docs/02_api_openapi.yaml` 側。
- 📌 **生成は `npm` の `postinstall` / `prestart` で自動的に走る**（`App/package.json`）。
  無いと `tsc` が通らないため、**忘れても困らないようにしてある**。

## 📌 `.memory/` — セッションを越えて残す記憶

**会話が切れても失われないよう、いまの状況をここに書き出す。**
⚠️ **書くのはAIの仕事**で、人は読むだけ。**指示を待たず自発的に最新へ保つ。**

| ファイル | 中身 | 消えるとき |
|---|---|---|
| [.memory/issues.md](.memory/issues.md) | **論点**（判断が必要なこと）。優先度（高/中/低）付き | **決めれば**消える |
| [.memory/todo.md](.memory/todo.md) | **作業**（やることが明確なこと） | **やれば**消える |

⚠️ **「決めること」と「やること」を混ぜない。** 決着したら `issues.md`「決着した論点」へ、
終わったら `todo.md`「完了したもの」へ。**ファイル間で移動させない**
（論点の決着で作業が生まれたら `todo.md` に足す）。

### いつ更新するか（このタイミングで必ず手を入れる）

| タイミング | やること |
|---|---|
| **セッションの最初** | **まず両方を読む** |
| 判断が必要なことが出た | `issues.md`「未決の論点」に優先度つきで追加 |
| 論点が決着した | `issues.md`「決着した論点」へ（作業が生じたら `todo.md` にも） |
| 作業が判明した / 終わった | `todo.md` に追加 / 「完了したもの」へ日付つきで移す |
| **セッションの終わり** | 記述と実態のズレを見直す |

⚠️ **「あとでまとめて」にしない。** 判明した時点で書く（セッションが切れたら失われる）。

### 書き方のルール

- **粒度は大きく。** 「US-2.03をやる」のような単位で書き、細かい手順は書かない。
- **一目で読める量に保つ。** `todo.md`「次にやる」は**3件程度まで**。
  `issues.md`「未決の論点」が増えたら**件数を削るのではなく優先度「高」から決める。**
- **やらないと決めたものは消す。** 優先度を下げて延命させない。
- **「完了したもの」「決着した論点」は消さない。** 到達点の記録として残す。
- **経緯・実測値は書かない。** 「何をやるか / 何を決めたか」と**参照先**だけ。
- ✅ **他ファイルへの参照は歓迎**（`pre-research/voice/` §6 のように節番号まで書く）。

### ⚠️ 参照の向きは一方通行（`.memory/` → 成果物）

```
.memory/（道具）  ──参照OK──▶  docs/ adr/ learning/ pre-research/（成果物）
                  ◀──禁止───
```

`.memory/` は**道具**（消えても作業しづらくなるだけ）で、他は**成果物**（消えたら困る）。

- ❌ **`docs/` `adr/` から `.memory/` を参照しない。**
  `.memory/` は**やれば消える・決まれば消える**ので、**成果物が壊れる**。
  未決と書くのは構わないが、**どこで管理しているかは書かない**。
  （例外: 各ディレクトリの `README.md` は開発者向け手順書なので参照してよい）
- ❌ **道具の都合で成果物を変えない。**「`.memory/` に書けないから `docs/` に書く」は成立しない。
- ❌ **進捗を `CLAUDE.md` に書き足さない。** 二重に書くと必ず片方が古くなる。

## ⚠️ 落とし穴（毎回踏みうるもの）

> **知らないと確実に事故るもの**だけ置く（設計は `docs/`、経緯は `adr/`、進捗は `.memory/`）。

- **Expo SDK 54 を使う。** SDK 57 は **Expo Go 非対応**。上げるときは要注意。
  ⚠️ **コードを書く前に SDK 54 の公式ドキュメント**
  （`https://docs.expo.dev/versions/v54.0.0/`）**を見る** —
  新しいSDKのドキュメントは、**ここに存在しないAPIを現役のように書いている。**
- **モデルIDは `us.anthropic.claude-sonnet-4-6`。** `jp.` は ap-northeast 専用で、
  us-east-1 からは "model identifier is invalid" になる。**Claude 5系は当アカウント未提供**。
- ⚠️ **AgentCoreはアイドル中も課金される**（文脈保持のためmicroVMが生存）。
- **AgentCoreの初回応答は10秒前後**（コールドスタート）。2回目以降は2〜3秒。
  **アプリ側では縮められない。**
- **`max_tokens` が厳しすぎると、回答が短くなるのではなく失敗する**
  （途中で切れ、壊れた部分応答が会話履歴に残る）。
  ⚠️ **Web検索のツール呼び出しも同じ枠を消費する**ので、読み上げる文章より余裕が要る。
- **アプリで 403 が出たら2つ疑う**: ①**APIキー**（未設定・誤り。アプリの設定画面）
  ②**デプロイ漏れ**（API Gateway は**未定義のパスに 404 ではなく 403** を返す）。
  ⚠️ **切り分けは `/health`**（キー不要）。200なら API は生きている。
- **`/health` 以外はAPIキーが必須。** キーの取り出し方は `Backend/README.md`。
  ⚠️ **クォータはポーリングも消費する**（1問 ≒ 11回）。
- **エージェントのソースは S3ソース(.zip)方式**（CodeZip）。Docker不要。

- Bedrock/Lambda等のClaude API実装を書く際は **`claude-api` スキルを参照**する
  （モデルIDやSDK仕様は記憶に頼らない）。

### スコープ（⚠️ 広げない）

**正本は [docs/00_user_stories.md](docs/00_user_stories.md)。** ❌ **やらないこと**は同 §5
（iOS対応・ナビ機能・複数ユーザー対応・凝ったUI等）。

- ⚠️ **メモ機能（US-X.01）は将来必要だが今は作らない。**
  ただし**後から `@tool` を足すだけで対応できる構造**は保つ。
- **会話継続の要件は「一問一答＋α」まで。**
  **15分以上あけて続きを求める使い方は想定しない**ので、
  **AgentCore Memory は採用しない**（[adr/003](adr/003_agentcore_as_orchestrator.md)）。
  「走行ログを残す」等が出たら、**会話の記憶ではなくDB連携**として別に設計する。
